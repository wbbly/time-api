# import "src/user/user.graphql"
# import "src/project/project.graphql"

scalar Date

type Time {
    id: ID!
    startDate: Date!
    endDate: Date
    title: String!
    timezoneOffset: String!
    user: User
    project: Project
}
